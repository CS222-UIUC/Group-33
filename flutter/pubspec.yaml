name: semaphoreci_flutter_demo
description: SemaphoreCI Flutter Demo

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.0.2
  google_fonts: ^3.0.1
  logger: ^1.1.0
  provider: ^5.0.0
  spotify_sdk: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  very_good_analysis: ^3.0.2

flutter:
  assets:
    - .env
  uses-material-design: true
